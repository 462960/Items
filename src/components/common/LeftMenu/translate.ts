const path = 'components.leftMenu';

export default {
  dashboard: { id: `${path}.dashboard` },
  structure: { id: `${path}.structure` },
  items: { id: `${path}.items` },
  inventory: { id: `${path}.inventory` },
  reports: { id: `${path}.reports` },
  planning: { id: `${path}.planning` },
  scanner: { id: `${path}.scanner` },
  adminPanel: { id: `${path}.adminPanel` },
};
