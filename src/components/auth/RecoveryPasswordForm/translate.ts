const path = 'routes.auth.recoveryPassword';

export default {
  btnSubmit: { id: `${path}.form.btn.submit` },
  confirmPassword: { id: `${path}.confirmPassword` },
  newPassword: { id: `${path}.newPassword` },
  title: { id: `${path}.form.title` },
};
